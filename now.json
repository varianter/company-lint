{
  "version": 2,
  "builds": [
    {
      "src": "./api/**/*.ts",
      "use": "@now/node@canary"
    }
  ],
  "env": {
    "IS_NOW": "true",
    "DB_CONNECTION_STRING": "@db-connection-string",
    "ACCESS_TOKEN": "@access_token"
  },
  "routes": [{ "src": "/api/(.*)", "dest": "/api/$1" }]
}
